pre-commit:
  commands:
    pint:
      glob:
        - "*.php"
        - "**/*.php"
      run: ./vendor/bin/pint
      stage_fixed: true

pre-push:
  commands:
    analyse:
      run: composer analyse
    test:
      run: composer test
