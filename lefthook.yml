pre-commit:
  commands:
    format:
      glob:
        - "*.php"
        - "**/*.php"
      run: composer format
      stage_fixed: true

pre-push:
  commands:
    analyse:
      run: composer analyse

    test:
      run: composer test
